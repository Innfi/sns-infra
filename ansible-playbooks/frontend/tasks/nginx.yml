---
# tasks file for nginx

- name: install nginx
  apt:
    name: nginx
    state: present

#- name: copy nginx config
#  copy:
#    src: files/https.config
#    dest: /etc/nginx/sites-available
#    mode: 0755

- name: start nginx
  systemd:
    name: nginx
    state: started
